---
export interface Props {
  href: string;
  className?: string;
  ariaLabel?: string;
  title?: string;
  tabindex?: string;
  target?: string;
}

const { href, className, ariaLabel, title, tabindex, target } = Astro.props;
---

<a
  type="button"
  href={href}
  class={`group ${className}`}
  aria-label={ariaLabel}
  tabindex={tabindex}
  target={target}
  title={title}
>
  <slot />
</a>

<style>
  a {
    @apply hover:text-skin-accent;
  }
</style>
